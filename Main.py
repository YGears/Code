import Config
"""
The main python file, 
from where our dreams come true and tears will be fed to the error in the terminal.

Used to run all classes and their relevant functions.
Objects will mostly be instantiated within Main.py, 
unless there is a dependency within the called class, in which case it will call the class itself.

Some files will end with the following code, in which case the code from the file will be ran instantly.
def call_functions():
    func_1()
    func_2()
    #..
    func_n()
call_functions()
"""

def Main():
    
    pass

Main()
